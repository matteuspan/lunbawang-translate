# Python
__pycache__/
*.pyc
*.pyo
.env

# Intermediate build files

# Training logs

# Runtime feedback files (written by serve.py, not for git)
feedback.db
feedback.jsonl

# Actively-written training log (races with the per-turn git hook while a
# background run is in progress) — committed as a finished snapshot instead
inkling_train.log
trainv2.log
orch.log
eval/val_bleu_cache/
eval_run_step*.log
